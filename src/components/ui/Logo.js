export const Logo = ({width = 20, height = 20}) => {
    return ( 
        <img 
            alt="Logo" 
            src="https://bit.ly/logo-ci0137" 
            className={`w-${width} h-${height}`} 
        />
    )
}